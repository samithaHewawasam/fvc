

-$PM_Receipt_No         = "";



































+    echo json_encde(array(

























+if (isset($_GET['RG_Dat'])) {










































+if (isset($_GET['RG_Banch_Code'])) {




















-$_SESSION['PM_Cheque_NO']          = $PM_Cheque_NO;








































































































-    $arguments = array(




































































































+ AND `SI_Reg_No` =? ORDER BY `SI_Ins_NO` ASC";





















-    // If there's money left
















+$response['string'] = "pqrs/StudentPdf.php?reg=9jv4kxpKCUupDPxWbSqC" . base64_encode($RG_Reg_N0);
-$response['string'] = "pqrs/StudentPdf.php?reg=9jv4kxpKCUupDPxWbSqC" . base64_encode($RG_Reg_N0);
+$getEleStatus = $helper->eligible($RG_Reg_N0);
+$getFullStatus = $helper->fulleligible($RG_Reg_N0, $_SESSION['PM_Amount']);
-$getFullStatus = $helper->fulleligible($RG_Reg_N0, $_SESSION['PM_Amount']);
+$response['eligible'] = ( $_SESSION['PM_Amount'] >= 3000 && ( !empty($getEleStatus) or !empty($getFullStatus) ) ? true : false);
-$response['eligible'] = ( $_SESSION['PM_Amount'] >= 3000 && ( !empty($getEleStatus) or !empty($getFullStatus) ) ? true : false);
+echo json_encode($response);
-echo json_encode($response);
+/*error capturing*/
-/*error capturing*/
+$errorCapture = array(
+    array(
+        'error' => "INSERT INTO `error_log`(`error`, `method`, `operator`, `branch`) VALUES (?,?,?,?)",
+        'bind' => array(
+            $response['errorInfo'],
+            'payment_master',
+            $RG_Operator,
+            $RG_Branch_Code
+        )
+    )
+);
-);


-$helper->errorCapture($errorCapture);

+?>
-?>
